@model MaintenanceContract
@{
    ViewData["Title"] = "Add Maintenance Contract";
}

<div class="row justify-content-center">
    <div class="col-lg-8">
        <h2 class="mb-4"><i class="bi bi-tools me-2"></i>Add Maintenance Contract</h2>

        <div class="card">
            <div class="card-body">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <div class="mb-3">
                        <label asp-for="LicensePurchaseId" class="form-label fw-semibold"></label>
                        <select asp-for="LicensePurchaseId" asp-items="ViewBag.LicensePurchaseId" class="form-select">
                            <option value="">— Select License Purchase —</option>
                        </select>
                        <span asp-validation-for="LicensePurchaseId" class="text-danger small"></span>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="ContractNumber" class="form-label fw-semibold"></label>
                            <input asp-for="ContractNumber" class="form-control" placeholder="Vendor contract number" />
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="AnnualCost" class="form-label fw-semibold"></label>
                            <div class="input-group">
                                <span class="input-group-text">$</span>
                                <input asp-for="AnnualCost" type="number" step="0.01" min="0" class="form-control" />
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="StartDate" class="form-label fw-semibold"></label>
                            <input asp-for="StartDate" type="date" class="form-control" />
                            <span asp-validation-for="StartDate" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="EndDate" class="form-label fw-semibold"></label>
                            <input asp-for="EndDate" type="date" class="form-control" />
                            <span asp-validation-for="EndDate" class="text-danger small"></span>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-4 mb-3">
                            <label asp-for="VendorContact" class="form-label fw-semibold"></label>
                            <input asp-for="VendorContact" class="form-control" placeholder="Contact name" />
                        </div>
                        <div class="col-md-4 mb-3">
                            <label asp-for="ContactEmail" class="form-label fw-semibold"></label>
                            <input asp-for="ContactEmail" type="email" class="form-control" placeholder="vendor@example.com" />
                        </div>
                        <div class="col-md-4 mb-3">
                            <label asp-for="ContactPhone" class="form-label fw-semibold"></label>
                            <input asp-for="ContactPhone" type="tel" class="form-control" placeholder="(555) 000-0000" />
                        </div>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Notes" class="form-label fw-semibold"></label>
                        <textarea asp-for="Notes" class="form-control" rows="3"></textarea>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-success"><i class="bi bi-check2 me-1"></i>Save</button>
                        <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts { @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); } }
